use_next_state: true
use_last_action: true

embadding_dim: 512
mixing_embed_dim: 32
hypernet_layers: 2
hypernet_embed: 64

# Prevent raw Unity coordinates/scales from exploding hypernetwork weights.
normalize_mixer_state: true
